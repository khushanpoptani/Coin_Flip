// Constants file for storing reusable values
const NETWORK = 'testnet'; // You can switch to 'mainnet' for production
const MIN_BET_AMOUNT = 0.0001; // Minimum bet amount in BTC

module.exports = { NETWORK, MIN_BET_AMOUNT };
